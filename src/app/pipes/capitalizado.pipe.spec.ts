import { ComponentsPipe } from './components.pipe';

describe('ComponentsPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentsPipe();
    expect(pipe).toBeTruthy();
  });
});
